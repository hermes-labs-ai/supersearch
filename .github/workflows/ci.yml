name: CI

on:
  push:
    branches: [main]
  pull_request:

# The test suite mocks all HTTP — no live network is required or used.
jobs:
  test:
    name: test (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e '.[test]'
          pip install 'ruff==0.15.14'

      - name: Check reviewed public source boundary
        run: python scripts/export_public_candidate.py --check

      - name: Lint
        run: ruff check .

      - name: Test (offline, mocked)
        run: pytest -q

  package:
    name: package
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"

      - name: Build and validate distributions
        run: |
          python -m pip install --upgrade build twine
          python -m build
          python -m twine check dist/*

      - name: Smoke test the installed wheel
        run: |
          python -m venv /tmp/supersearch-wheel
          /tmp/supersearch-wheel/bin/pip install dist/*.whl
          cd /tmp
          /tmp/supersearch-wheel/bin/supersearch --help
