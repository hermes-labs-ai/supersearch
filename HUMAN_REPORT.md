# Public boundary cleanup

The current tree mixed useful product documentation and bounded evaluation with
private component details, local setup, review handoffs, and publication policy.
The export filter also admitted whole directories and omitted newer integrations.
All 27 commits reachable from the audited public main were inspected; Gitleaks
reported no credentials. Prior commits and published archives retain old content.

Removed operating details from the current docs, demo, code comments, and
normalized evaluation reports. Preserved the search API, runtime prompts,
licensing, source mechanisms, query receipts, usefulness labels, and limitations.
Historical redactions are labeled; hashes identify the current public copies.
Fixed stale evidence references and contributor guidance. The host trial harness
now accepts model options and uses portable query commands. Exports use an exact
file list, preflight checks, and a CI check; source archives explicitly select
docs, tests, and evaluation files.

The resulting public boundary is product mechanisms, interfaces, contributor
instructions, and reproducible evidence. Raw agent-host logs remain unavailable;
the normalized reports say so. Legacy scaffold identifiers remain for API
compatibility. No package functionality or prompt text was removed.

Validation: 289 offline tests passed; two opt-in live-model tests skipped. Lint
and initial package builds passed. Final export, installed-package validation,
and publication state are recorded in `ACTION_MANIFEST.json`.

**Decision for Roli:** whether to pursue removal of historical material from old
commits, immutable tags, or published archives. That would require separate
scope and authority and cannot retract existing copies. No history rewrite,
visibility change, license change, or versioned package release is included.
